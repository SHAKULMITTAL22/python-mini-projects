# ********RoostGPT********
"""
Test generated by RoostGPT for test python-test-1 using AI Type Azure Open AI and AI Model roostgpt-4-32k

Test generated by RoostGPT for test python-test-1 using AI Type Azure Open AI and AI Model roostgpt-4-32k

ROOST_METHOD_HASH=__init___edbb34f08d
ROOST_METHOD_SIG_HASH=__init___bba406a912


```
Scenario 1: Test file directory creation
Details:
  TestName: test_directory_creation
  Description: This test is to verify if a directory "file_split" is created after the function is initialized.
Execution:
  Arrange: Provide a filename and split number to initialize the function.
  Act: Initialize the function which should create a directory.
  Assert: Check if the specified directory exists.
Validation:
  The function as part of its execution creates a directory "file_split". This test will validate whether the creation operation is performed successfully.

Scenario 2: Test split number initialization
Details:
  TestName: test_split_num_initialization
  Description: This test is to verify the proper initialization of the split number.
Execution:
  Arrange: Initialize the function with a filename and a split number.
  Act: Check the value of self.split.
  Assert: The value of self.split should match the provided split number.
Validation:
  The proper initialization of members is fundamental to class functionalities. This test ensures the split number is properly initialized.

Scenario 3: Test directory deletion and creation
Details:
  TestName: test_directory_deletion_creation
  Description: This test is to ensure that if the directory "file_split" exists, it is removed and a new directory is created.
Execution:
  Arrange: Create a directory "file_split" if it doesn't exist. Then, initialize the function.
  Act: Upon initialization the existing directory should be removed and a new one should be created.
  Assert: Check if the directory creation time has changed (if it’s the same folder, the creation time should not change).
Validation: 
  The directory being tested needs to be created stale and empty at the start of every function initialization. This test validates if an existing directory is cleared before processing.

Scenario 4: Test file extensions based on filenames
Details:
  TestName: test_file_extension_assignment
  Description: This test is to verify if file extensions are identified and assigned correctly.
Execution:
  Arrange: Initialize the function with a filename, where the filename has a '.txt' or other extension.
  Act: Check the value of self.file_extension.
  Assert: The value of self.file_extension should match the extension of the provided filename.
Validation:
  Correctly identifying file type (.txt or .csv) is crucial for further processing; the correct identification and assignment of file extensions validate the function's usability and accuracy.
"""

# ********RoostGPT********
import os
import shutil
import pytest
from split_files import Split_Files

class Test_SplitFilesInit:
    """
    This test class contains the unit tests for the Split_Files functionality.
    """

    # Test directory creation
    def test_directory_creation(self):
        """
        Test case for checking whether the directory is being created or not.
        """
        # setup
        file_name = "test_file.txt"
        split_number = 5
        # action: Initialize the Split_Files function
        split_file = Split_Files(file_name, split_number)
        # assertion: Check if the directory is created
        assert os.path.exists(split_file.directory) == True

    # Test split number initialization
    def test_split_num_initialization(self):
        """
        Test case for checking whether the split_number is initialized properly or not.
        """
        # setup
        file_name = "test_file.txt"
        split_number = 5
        # action: Initialize the Split_Files function
        split_file = Split_Files(file_name, split_number)
        # assertion: Check if the split_number is initialized correctly
        assert split_file.split == split_number
        
    # Test directory deletion and creation
    def test_directory_deletion_creation(self):
        """
        Test case for checking whether the existing directory is deleted and new one is created or not.
        """
        # setup
        file_name = "test_file.txt"
        split_number = 5
        # Create the directory if it doesn't exist
        if not os.path.exists("file_split"):
            os.mkdir("file_split")
        # Get the creation time of the directory
        creation_time_before = os.path.getctime("file_split")
        # action: Initialize the Split_Files function
        split_file = Split_Files(file_name, split_number)
        # Get the creation time after initialization
        creation_time_after = os.path.getctime(split_file.directory)
        # assertion: Check if the creation time has changed i.e. directory is deleted and recreated
        assert creation_time_before != creation_time_after
        
    # Test file extensions
    def test_file_extension_assignment(self):
        """
        Test case for checking whether the file_extension is assigned correctly or not as per the file-type.
        """
        split_number = 5

        # setup for txt file
        file_name = "test_file.txt"
        # action: Initialize the Split_Files function
        split_file = Split_Files(file_name, split_number)
        # assertion: Check if file_extension is assigned correctly
        assert split_file.file_extension == ".txt"

        # setup for csv file
        file_name = "test_file.csv"
        # action: Initialize the Split_Files function
        split_file = Split_Files(file_name, split_number)
        # assertion: Check if file_extension is assigned correctly
        assert split_file.file_extension == ".csv"
